# stock-etsy-service

Go microservice for Etsy shop connection and product inventory synchronization.

Local service URL:

```text
http://localhost:8082
```

Health endpoint:

```text
GET /health
```
